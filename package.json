{
  "name": "agent-profile",
  "private": true,
  "scripts": {
    "dev": "pnpm -r dev",
    "demo": "pnpm --filter demo dev",
    "build": "pnpm -r build"
  },
  "devDependencies": {
    "typescript": "^5.5.0"
  }
}
